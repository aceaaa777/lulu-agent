# 本轮交接：独立Agent第一版

2026-09-07，用户明确授权先做独立Agent，长期记忆必需，不连接演示层。

已在本目录实现并运行；复用固定nanobot AgentRunner与工具框架，本地Qwen2.5 7B。没有修改桌面动画版本。

当前入口：http://127.0.0.1:8766；双击“启动 Lulu Agent.command”可启动Mac版。

已完成14项回归测试、7步真实模型/固定程序混合端到端测试、公开网页搜索读取、界面提交到真实文件的验收。测试只说明所列样例通过。真实报告见验收记录.md和live-test-report.json。

长期记忆：本机SQLite、带原话来源、相关性检索、同名纠正、删除。删除一条目前会清空全部对话正文和任务上下文；这是明确告知的原型限制，尚未实现精准删除依赖图。不是向量语义记忆或模型训练。

Windows测试包在artifacts；包不含Python/Ollama/模型权重，尚不是一键EXE。用户问VM是否值得搭，建议用Windows11 VM测试兼容性，再用8GB/16GB真实Windows x64电脑测性能。尚未安装VM或声称Windows验收通过。

本机是Apple M5、24GB，不能把本机7B速度当成低配Windows表现。7B开放任务仍可能遗漏或理解错误；已补工具回执校验、Qwen文本式调用适配、常见固定流程、原句要求检查、总结日期检查，仍不是通用事实正确性保证。

下一步等用户试用反馈，重点验证记忆体验和任务可靠性；不要擅自连接演示层或改成在线模型。

## 17:35 用户实际试用修复
用户的“正文+把上面的话写成pdf”和跟进“上面那段话写成word”均失败。前者未调用工具，后者编造/tmp文件路径。已新增对话原文导出解析与跨轮来源追踪，直接生成文档，无需7B规划；增加3项回归，共17项通过。已在原会话补生成用户要求的PDF与DOCX并检查正文。原失败记录保留，不能把此前测试理解成通用可用性验收。

## 2026-09-07 18:00 Harness迁移

用户明确选择DeepSeek Harness底座。当前8766已运行0.2，入口lulu/agent.py导出HarnessAgent；官方source CLI sdk-minimal，固定上游d347e703908d0406b7a7ef80e3a0e594d86b2215。无上游修改，无演示层集成，旧数据库保留。参见Harness迁移验收.md与README.md。生产进程使用当前源码，不是旧nanobot runner。

真实服务续办了截图请求，任务61276bc6746e41c9完成，workspace/学习计划.docx可下载。UI已刷新为试用版02。新6项Harness集成、1项HTTP、16项旧能力回归通过；真实模型5种用法通过（新建文档另在实际服务复测）。Windows/精简打包尚未完成，不要把旧Windows zip说成新版本。下一步应先补新底座其它能力实际验收，再精简runtime和Windows VM验证。


## 2026-09-07 18:40 低资源离线验收完成

用户要求验证低配、完全本地、复杂任务可靠并搭VM。已使用现有Docker Linux ARM VM搭隔离环境，未搭Windows VM。正式控制组4CPU/6GiB/无swap/无外网/CPU-only/4线程，6项组合与边界用例0完整通过；2CPU/4GiB/2线程两项加载均OOM，0/2。模型不变Qwen2.5:7b。4CPU组发生1次模型OOM。报告《低配离线验收报告.md》详列假完成、起草提示污染、漏传工具结果、错误残留和否定句误判。生产源码未改，不能归因所有7B能力不足。原始结果validation/vm/results。初始环境失败和自动线程偏差记录单独保存。后续先修已定位实现问题，再复测同组用例；Windows/实机仍待验证。
