# 第一版验收记录

日期：2026-09-07。环境：Apple M5 / 24GB内存 / macOS ARM64 / Python 3.12。

默认模型：本机 Ollama `qwen2.5:7b`，Q4_K_M，8K上下文；实际模型文件约4.68GB。

## 回归测试

14项通过（记忆持久化、纠正删除、中文检索、上下文预算、任务续办、文件路径限制、备份恢复、文档读写、统计、提醒、执行失败恢复、虚假完成拦截、本机API边界）。

有1条aiohttp建议使用AppKey的非失败警告。

## 端到端任务

| 任务 | 本轮耗时 | 执行方式 |
|---|---:|---|
| 请记住：我的报告必须使用中文，先给结论，再列依据。请保存为长期记忆。 | 6.95秒 | 真实7B + 工具 |
| 我以前对报告的语言和结构有什么要求？ | 4.87秒 | 真实7B + 工具 |
| 创建 学习计划.md，内容是一个三天阅读计划，并在文件中明确写出“每天阅读30分钟”。实际保存文件。 | 11.29秒 | 真实7B + 工具 |
| 把 学习计划.md 中的“每天阅读30分钟”改成“每天阅读45分钟”，再转换成 学习计划.docx。 | 0.07秒 | 固定程序流程，无模型推理 |
| 分析 销售.csv 的金额总和与平均值。请使用工具计算。 | 0.00秒 | 固定程序流程，无模型推理 |
| 读取 学习计划.md，简要总结文件中的阅读安排。 | 8.33秒 | 真实7B + 工具 |
| 10分钟后提醒我休息 | 0.00秒 | 固定程序流程，无模型推理 |

这是一轮小样本验收，不是成功率或性能基准。开发中出现过漏参数、文本式工具调用、提前结束以及总结增加日期的问题；已针对发现的问题修复并复测，不能据此保证所有开放任务都正确。

统计验证检查真实工具回执，不只比较模型回答的数字。文件验收检查真实产物及用户要求的原句；总结验收核对读取来源与三天安排。

## 网络与界面

公开网页搜索返回SQLite官方文档；直接网页读取返回HTTP 200。详细记录在network-test-report.json。

独立本机面板提供聊天、记忆、文件、任务、提醒；已从面板提交生成“验收示例.md”，验证成功回复、文件列表和实际文件内容一致。

## 未验证与限制

- 尚未运行Windows VM或Windows实机。
- 当前Windows包是源码与依赖安装脚本，不是免安装EXE。
- 记忆删除目前会清空所有对话正文和任务上下文，用户文件和其他记忆保留。
- 文档转换是基本内容转换，复杂排版、OCR、公式重算不在此版范围。
- 提醒当前只在面板和页面标题展示，需要程序及页面运行。
- 7B对复杂开放任务仍需进一步验收；任务完成标记不等于语义正确性保证。

## 资源观察

后台启动后曾采样到约41MB RSS（不含Ollama、浏览器和后续文档模块，非峰值）。当前Mac项目虚拟环境约235MB。这些数字不能作为Windows整套应用占用承诺。
