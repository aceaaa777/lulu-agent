# DeepSeek Harness 迁移验收 · 2026-09-07

状态：0.2 开发试用版已启动于 http://127.0.0.1:8766/ ，没有接入桌宠演示层。本机模型仍为 Ollama qwen2.5:7b。官方上游提交 d347e703908d0406b7a7ef80e3a0e594d86b2215，未修改上游源码。

## 实际迁移

生产入口 lulu/agent.py → HarnessAgent → 官方 Python SDK → 官方 dsh --profile sdk-minimal → Lulu 插件。原 nanobot AgentRunner 不在生产执行路径中；不自动回退旧底座。仍复用原本地文件能力、网页工具、工具校验、SQLite记忆与任务存储。

Harness通过本地鉴权接口调用Ollama，采用8192上下文、最多1600输出token，每轮最多7个模型步骤（第8步开始拒绝），最多一次完成前修正提醒，总任务等待上限240秒。起草工具另调用一次无工具模式的本机模型生成正文。记忆检索、近期对话和任务上下文仍有限额；未加入向量模型。工具操作成功不等于全文语义正确，当前验收检查真实产物、可读正文及指定文件名，不能自动证明内容优质。

文件生成后重新打开并记录SHA-256；同一任务重复调用相同创建参数只返回已有结果。工具失败进入Harness日志，模型可以修正后继续。取消任务后等待运行时退出和已接纳文件操作结束，再释放任务锁。临时Harness会话在每轮结束后清理，SQLite是长期记忆来源；删除记忆时不会重新从隐藏Harness历史召回。

## 已运行验证

- 新底座真实profile＋脚本模型：6项通过。文档生成、无产物不能报成功、读不存在的文件后恢复新建、记忆重启与删除、重复创建幂等、取消释放进程与锁。
- HTTP接口回归：1项通过。鉴权、来源、路径限制、记忆导出删除。存在1个aiohttp AppKey写法提示，不影响行为。
- 复用旧能力回归：16项通过。这些采用旧核心，不能充当新Harness自然语言成功率。

真实本机7B模型（隔离数据库）：

|请求|结果|耗时|
|---|---|---|
|正文＋把上面的话写成pdf|已生成并读取验证原文|7.64秒|
|上面那段话写成word|已生成，正文逐字一致|9.75秒|
|记住：我喜欢简洁中文|已写入SQLite|7.44秒|
|重开存储、新会话询问风格|正确回忆简洁中文|5.47秒|
|生成一份下周学习计划，保存为学习计划.docx。|954字正文，真实DOCX|45.87秒|

实际8766试用服务续办截图失败请求：任务61276bc6746e41c9，57.41秒完成；学习计划.docx为37489字节，1172字符，含周一至周日，日期2026年9月14日至20日。已在浏览器确认版本02和下载按钮。原失败任务记录保留。

修正期间也出现过文件名验收误判、空正文、日期错误、占位内容和重复创建，未将这些失败计为通过。拆分正文起草与工具选择后完成上述测试。样本有限，不能据此宣称自然语言请求普遍可靠。生成的计划使用通用假设，尚未根据用户科目与可用时间定制；文档转换为基本文本重排。

## 尚未验收

新底座下完整搜索、提醒、表格分析等能力尚未逐一进行真实模型验收。Windows VM尚未搭建，Windows启动脚本尚未实际执行。当前源码开发依赖约1.4GB，不是低配Windows正式发行包；旧v0.1 Windows压缩包不含此次迁移。精简运行包、Windows兼容测试和真实低配电脑性能仍需完成。
