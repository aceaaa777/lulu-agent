# 第三方组件

Lulu Agent 的后端依赖以下 Python 包（许可证见各项目）：aiohttp（Apache-2.0）、httpx（BSD-3）、jsonschema（MIT）、python-docx（MIT）、openpyxl（MIT）、pypdf（BSD-3）、reportlab（BSD）、filelock（Unlicense）。

运行组件：Ollama（MIT）、Godot Engine（MIT）、Qwen3 模型权重（Apache-2.0，由用户通过 Ollama 自行下载，不随包分发）、PyInstaller（GPL 附例外，冻结产物不受影响）。

桌宠动画帧（frames.pck）随 Release 分发，版权归项目作者。
