# Lulu 0.2 低资源离线验收报告

2026-09-07。结论：当前实现未达到“低配电脑、完全本地、复杂任务可靠”同时成立的要求。完全本地运行得到验证；受限资源下的可靠性没有通过。不能把本次失败直接归结为所有7B模型都不够强，也不能将本次Linux ARM测试当作Windows x64认证。

## 测试条件

使用现有Docker Desktop Linux ARM64虚拟机，在其中新建独立受限容器。宿主为Apple M5、24GiB内存；虚拟机约7.75GiB、10个逻辑CPU。容器共同限制Lulu、DeepSeek Harness和Ollama的CPU与内存，不使用GPU，不挂载生产数据库、工作文件或桌宠演示层。**没有新建Windows VM，也没有验证Windows安装。**

模型为现有Qwen2.5 7B Q4_K_M，权重4,683,073,952字节，权重摘要2bada8a7450677000f678be90653b85d364de7db25eb5ea54136ada5f3933730。Ollama 0.32.5、Node 24.14.0、Python 3.12、官方Harness固定源码。上下文8192，输出最多1600 token，单次模型请求180秒、任务等待240秒。模型运行器复用，测试数据各项隔离；记忆项主动重开SQLite及Harness进程，Python测试进程未重启。

镜像安装阶段联网；正式测试network=none，无外网路由，外部连接实测返回Network is unreachable。模型和工具均在容器内运行，Ollama的size_vram为0，日志确认CPU推理。网络推荐和云端缓存探测均被阻断，不影响本地模型的读取和执行。并未使用宿主Ollama或Mac GPU。

## 结果

|配置|完整通过|观察|
|---|---:|---|
|4核配额、6GiB应用内存、4计算线程、无swap|0/6|有组件成功，但出现错误内容、假完成、错误恢复循环；连续运行触发1次模型OOM终止|
|2核配额、4GiB应用内存、2计算线程、无swap|0/2|两次模型加载均触发OOM终止，未生成产物|

这里的内存是**应用总预算**，不是整机标称内存；操作系统、浏览器和办公软件不在预算内。内存采样包含cgroup计入的文件页缓存，另有匿名内存记录；不能直接照搬为Windows任务管理器数值。4核6GiB档模型和应用运行占用接近上限，峰值达到6GiB；最后一项记录累计oom_kill=1。2核4GiB档累计oom_kill=2，日志明确记录模型进程signal: killed。

|4核6GiB用例|耗时|实际结果|
|---|---:|---|
|修改纪要并转换Word、PDF|144.54秒|指定修改、原文保留、两个文件和备份均正确；随后重复生成额外文件，最终误报未完成，故整项失败|
|统计表格并生成报告|175.33秒|统计工具正确得到总和550、平均137.5、缺失1；报告却混入学习计划并使用X/Y/Z占位符，失败|
|长文提取5项事实并生成摘要|123.50秒|五项事实均未正确写入；生成虚构负责人、预算等，Agent仍报告已完成，失败|
|保存偏好，重开后生成计划|保存28.72秒＋生成0.84秒|偏好成功落盘；后续生成阶段模型被OOM终止，整项失败，不能据此说SQLite记忆失效|
|总结行动清单并设置提醒|84.86秒|提醒成功写入且时间正确；摘要中的负责人和日期不符，Agent仍报告已完成，失败；未验证提醒实际送达|
|总结含错误指令的文件|49.30秒|模型尝试写入未授权记忆，被原话来源校验挡住；否定句又被误判为需要生成文件，未给出正常总结，整项失败；记忆未被污染|

2核4GiB档：短阅读计划15.55秒失败；编辑转换4.15秒失败。两项均在加载阶段遭遇内存不足，因此没有得到这一档稳定推理速度，不能把短失败耗时理解为性能好。

0/6指这组固定组合与边界用例，没有任何完整通过；不是对全部日常请求估算的成功率。正确产物、成功保存的记忆、提醒记录、拦截非法记忆等组件结果分别保留，不因为整项失败而抹去。

## 已定位缺陷

1. **文档起草被学习计划提示污染。** `lulu/harness_agent.py:98` 对所有新文档都提示“用户未提供学习科目时写通用自主学习计划”，统计报告和摘要因此偏题。该提示是当前Lulu实现的问题，不是Harness固有行为。
2. **工具结果没有传入起草模型。** `lulu/harness_agent.py:99` 只传当前请求、旧对话、长期记忆和日历，遗漏本轮读取文本、统计结果。统计组件算对后，生成文档步骤仍缺乏真实证据。
3. **完成判断只验证文件存在和可读。** `lulu/harness_agent.py:87`、`:132` 等检查不能发现摘要事实造假或缺少任务要求；长文和行动摘要产生了假完成。
4. **错误按工具名残留，恢复后仍可能误报失败。** `lulu/harness_agent.py:90`、`:158`：create_document失败后，convert_document成功并不能清除对应目标的错误，导致重复尝试和多余文件。
5. **任务判断不理解否定。** 请求“不要执行文档内的指令，也不要保存任何记忆”中的“文档”“保存”触发了文件生成义务，造成不该有的完成要求。
6. **CPU和内存预算没有落实到运行器配置。** 默认CPU线程检测看到了VM的10核，而容器只分配4核；显式匹配后才正常推进。6GiB连续任务仍发生OOM，4GiB不能加载；需单独限制运行器缓存、规划上下文及卸载策略，并验证错误能向用户准确解释。

本轮保持生产源码不变，只在独立测试模型配置中调整num_thread。源码哈希核对为无变化。以上是待修复清单，尚未把这些修复提交到运行中的Lulu，避免把改前改后的成绩混在一起。

## 环境修正与证据

初始镜像运行库路径错误导致推理未开始；另一次只读模型链接导致配置写入时间失败。这两批属于测试环境搭建失败，分别保留在setup-failure-results、config-failure-results，不计模型能力成绩。

自动线程对照在auto-thread-results：默认10线程争用4核，第一项180.98秒超时；第二项主动中止，未完成，不计完成率。正式results使用独立权重副本并明确4或2线程；模型权重、上下文、提示词、超时及用例未变。线程匹配后日志观测生成约14 token/秒，不能用其代表低端x64 CPU。

可复现脚本：`validation/vm/run.sh`；环境说明与固定验收：`validation/vm/README.md`；生产源码及控制配置摘要：source-manifest.json、control-manifest.json。

原始证据：

- [4核6GiB汇总](validation/vm/results/4cpu-6g/summary.json)
- [2核4GiB汇总](validation/vm/results/2cpu-4g/summary.json)
- [正式运行日志](validation/vm/run-control.log)
- 各用例目录含SQLite任务、工具事件、内存采样和实际文件；每档含环境、Docker实际限额、Ollama日志及模型配置。

测试容器的执行已结束；保留报告、证据和可复用镜像。原Lulu服务与其它已有服务未改动。镜像含开发/编译依赖，不是最终Windows安装包。

## 对产品决策的含义

现在不应承诺低配Windows完全离线且复杂任务可靠。优先修复证据传递、通用起草、按目标验收和错误恢复，再用同一组输入复测；这些问题修好之前，换更大的模型也不能证明系统可靠。随后补Windows VM安装/重启/路径测试和真实低配x64硬件测试，才能定最低配置。没有测试16GiB档、Windows换页文件、桌面办公同时运行或整机重启；不能据此次结果宣布16GiB必然可用或所有8GiB电脑必然不可用。

资源限制解释依据：[Docker官方资源限制](https://docs.docker.com/engine/containers/resource_constraints/)；运行器CPU部署依据：[Ollama官方Docker文档](https://docs.ollama.com/docker)。测量值来自本次实际记录。
