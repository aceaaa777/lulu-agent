# Windows 验收方案

建议搭 VM，但先用它解决兼容性，随后用真实低配机决定性能档位。

## 第一阶段：Windows 11 VM

建议测试配置：4个虚拟CPU、8GB内存、80GB磁盘，另建16GB内存对照快照。这是测试建议，不是产品最低配置承诺。

安装、首次启动、中文和空格路径、无管理员权限、模型缺失、断网、重启、取消、文档依赖、端口占用，是VM首先要验证的内容。

微软列出的 Windows 11 系统门槛不等于 Lulu + 7B 的可用配置。VM需满足UEFI、安全启动和TPM等要求：
https://learn.microsoft.com/en-us/windows/whats-new/windows-11-requirements

Apple Silicon 上运行的 Windows ARM VM，应单独记录为ARM测试结果。它不能代替主流Windows x64验收，也不能拿Mac加速推理速度代替低配Windows CPU推理速度。

## 第二阶段：真实低配Windows x64电脑

选择8GB、无独显机器；16GB机器作对照。默认仍是Qwen2.5 7B量化版。记录系统可用内存、后台占用、模型占用、首轮耗时、连续响应耗时、峰值占用和任务成功率。

若8GB体验不达标，再基于证据讨论更小模型或可选在线推理，不预先把用户的7B要求改掉。

## 相同验收任务

1. 记住报告偏好，关闭程序重开，在新对话回忆。
2. 修改偏好，确认旧要求不再进入上下文。
3. 删除记忆，确认重启后不会从对话或任务摘要恢复。
4. 生成文档、修改文字、转换为DOCX/PDF、恢复备份。
5. 分析表格总和、平均值、缺失值。
6. 对长文分段读取、总结，保留来源。
7. 任务中止、重启、选择继续，核对已生成文件。
8. 到时提醒、重复提醒、关闭程序后补显示。
9. 本地检索断网可用；联网搜索失败时明确报错。
10. 同时打开浏览器和办公软件，记录实际交互是否卡顿。

运行“运行测试.cmd”会执行回归和真实7B测试。真实模型用临时目录，结果保存在 `live-test-report.json`。

目前未创建或运行Windows VM，也未在Windows实机验收；不得把Mac测试记录作为Windows通过结论。
