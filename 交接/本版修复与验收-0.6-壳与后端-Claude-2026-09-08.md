# Lulu Agent 0.6 壳与后端版（Claude，2026-09-08）

改动对象：`Lulu完整预览版_2026-09-07_动画资源优化/02_开发源码/lulu-agent`。这一版按 9 月 7 日晚定下的路径做：**Lulu 是壳，模型是用户自己的选择**。桌宠动画与演示层逻辑未动；工作窗口加了一页"模型"，其余页面不变。

## 定下的路径（对照执行）

| 决定 | 做法 |
|---|---|
| 全换 Qwen3，只做 4B 和 8B | 4B 档两个模型 `qwen3:4b-instruct-2507-q4_K_M`（作答）+ `qwen3:4b-thinking-2507-q4_K_M`（深度思考）；8B 档没有拆开的 2507 版，用混合型 `qwen3:8b` 加 think 开关。按内存自动选档（≥12GB → 8B）。Ollama 库里更新的 Qwen3.5（4B/9B）作为自定义项留给你实测，没有默认押它。 |
| 壳的能力：装好后可通过 API / CLI / claude -p / 本地模型调用并显示状态 | 新增 `lulu/backends.py`：四种后端一个接口，`config.json` 一个字段切换；钥匙单独存 `data/secrets.json`（0600），不进 config、不进安装包。状态接口 `GET /api/backend`，切换 `POST /api/backend`，体检 `POST /api/backend/probe`，试一句 `POST /api/backend/test`，存钥匙 `POST /api/backend/secret`。 |
| 主推 7B（=8B 档），重在总结、提醒、翻译、提取、闲聊，搜索能加就加 | 新增翻译技能；总结/提取/提醒/闲聊/记录/记忆问答保留并收紧路由；搜索改成可插拔提供方（博查 / Tavily / Brave 官方 API + 无钥匙抓取兜底），用户填哪家钥匙走哪家。 |
| 推理默认关闭，UI 可开启 | `think` 是每次调用的参数，只作用于生成类槽位（闲聊、总结、写文档、翻译、联网问答的最后作答）；分类、抽取、核对永远不思考。工作窗口"模型"页有"深度思考"开关，随时可切，不用重启。思考文本不进答复、不进记录正文。 |
| 生成文档必须有材料和要点 | `has_materials()` 门槛：有指定文件、有要点（句内或答复里）、引用了 Lulu 自己的历史、明确要模板、或需要联网取材，四者有其一才动笔；否则零模型调用，直接问"要写什么内容？"并给两个选项（用工作文件夹里的文件 / 先给一个空模板）。 |
| 接口用户自己看着办，我们给脚本 | `安装本地模型.command` / `.cmd`（装 Ollama、按内存拉档位模型、写设置、体检），`后端体检.command` / `.cmd`，`python -m lulu.backends status|probe|set|secret`，`python -m lulu.searchapi <查询词>`。启动器不再因为没有 Ollama 而拒绝打开：走接口或命令行时不启动本地模型；本地模型缺失时照常开壳，状态栏提示去"模型"页体检。 |

## 结构

```
lulu/backends.py    后端配置、档位、钥匙、构建 provider、体检、命令行工具
lulu/searchapi.py   搜索提供方：bocha / tavily / brave / engines（无钥匙抓取）
lulu/models.py      OllamaProvider（两模型或混合 + think）、OpenAICompatibleProvider（enable_thinking / 推理模型名）、
                    CommandProvider（任意命令行）、ClaudeCliProvider（claude -p --output-format json）
lulu/skills.py      route_rules() 规则优先路由；translate 技能；generate 的材料门槛；query 的"问句即查询"与无资料回退
lulu/loop.py        Agent.set_provider / set_think；classify() 规则命中时不调模型；think 传入生成槽位
lulu/research.py    research(searcher=…)：提供方给的答案也当一页可引用原文
lulu/server.py      /api/backend 系列；/api/desktop 带 backend 状态；版本 0.6-preview
desktop/src/pet.gd  新增"模型"页；状态栏显示后端·模型·深度思考；任务进行中提示"深度思考中"
```

### 路由：规则优先，模型兜底

`skills.route_rules()` 先看决定性句式，命中就直接定技能、**不调分类模型**（省一次 7B 调用，也少一次判错）。顺序：提醒 → 记忆问答 → 记录 → 转换 → 翻译 → 总结（总结动词在生成动词之前且有材料对象）→ 生成（文件契约）→ 本地查找 → 关于 Lulu 自己的话 → 查询 → 任何问句（吗/呢/？/什么/怎么/听说/是真的吗…）→ 短句闲聊。一句话里两件事（并且/然后/顺便 + 两个技能词）交给分类器和通用循环。

"xx是真的吗""听说xxx"按你的意思算查询：有搜索钥匙就联网并按核对过的原文作答；没资料且不是实时类问题，就用模型自己的知识回答并在末尾标注"没有联网核对到资料"；实时类（今天/最新 + 价格/新闻/天气）资料不足仍然暂停提问，不编数字。

### 翻译

`translate`：目标语言从句子里取（翻译成英文 / 英文怎么说 / 译为日文），取不到就按原文判向（中文→英文，其他→中文）；原文来自引号、"翻译：" 之后、指定文件或上一条回答；长文按段切块逐段译。验收：数字必须原样出现、长度比例在 0.15–6 之间，不过重译一次再标注。指定文件时写 `译文-<原名>.<md|txt|docx>`，否则直接回。

### 深度思考

| 后端 | 关 | 开 |
|---|---|---|
| Ollama 4B 档 | 调 instruct 模型，不传 think | 调 thinking 模型，`think: true`，`num_predict` 多留 1500 |
| Ollama 8B 档（混合） | `think: false` | `think: true` |
| API（通义等） | 不传 | `enable_thinking: true`（可在 config 里改参数名；DeepSeek 预设改为切到 `deepseek-reasoner`） |
| claude -p / 其他命令行 | 不支持，开关无效 | 同左 |

## 验证

脚本模型回归 126 项通过（`python3 -m pytest tests -q`），新增 `tests/test_backends.py`（配置、档位、钥匙权限、两种 think 载荷、命令行后端真实子进程、体检、搜索解析、研究链路用提供方答案）、`tests/test_http.py` 新增后端接口用例、`tests/test_skills.py` 新增规则路由表（含你举的"是真的吗/听说"）、翻译四例、无资料回退、材料门槛四例、think 只进生成槽位。

`desktop/src/pet.gd` 这次用 Godot 4.4.1 headless 做了完整的解析加语义检查（未声明变量会报错的那种），通过；仍未在真机上跑过，第一次启动请看 `desktop.log`。

**真实模型仍未跑过。** 在 Mac 上：

```
cd 02_开发源码/lulu-agent
./安装本地模型.command            # 按内存拉 Qwen3 档位并体检；或 ./安装本地模型.command 4b
.venv/bin/python -m pytest tests -q
.venv/bin/python tests/live_acceptance.py --tier 8b --only 12,13,14,15,17,18,20,21,22,23,24,25,27
.venv/bin/python tests/live_acceptance.py --tier 4b --only 12,14,21,22,24,25,27
.venv/bin/python tests/live_acceptance.py --backend claude_cli --only 12,14,21,25   # 走 claude -p
.venv/bin/python tests/live_acceptance.py --online --only 8,10,11,24                 # 填了搜索钥匙后
```

结果在 `live-acceptance-report.json`，发我即可继续。装依赖仍用"从源码启动 Lulu（新Agent）.command"（它会建 .venv）；该脚本已改为没有 Ollama 也能启动。

## 已知边界

8B 档在 8GB 机器上会很挤，安装脚本按内存自动落到 4B；Qwen3.5 未验证；命令行后端不支持工具调用（技能表几乎不需要）；API 的 `enable_thinking` 参数名按通义写，其他服务商可在 config.json 的 `api.think_param` 改或留空；博查/Tavily/Brave 的响应结构按官方文档写的解析器，我这边没有钥匙实测，`python -m lulu.searchapi bocha 今日新闻` 一跑便知；搜索接口在大陆能否直连要你实测；`claude -p` 每次把整段提示当参数/标准输入传给命令，长提示（>10k 字）没试过。

## 0.6.1 补丁（2026-09-08，按第一次真机会话修）

真机会话里的三处"傻"都不是模型：

1. **"本机服务连接中断，正在重连…"**（问"我听说openai越狱了？"时）——`websearch.check_url` 在异步抓取里用了同步的 `socket.getaddrinfo`，DNS 一慢整个事件循环卡住，启动器连续三次健康检查失败就把 Agent 重启了。这也是 0.5 日志里"Restarting agent"×3 的来源。改为线程内解析、6 秒封顶，网页解码与去标签也移到线程。
2. **"帮我把上一份文件翻译成英文" → "The previous document"**——翻译技能没有解析"上一份文件"这类指代，把这四个字当原文译了。新增 `referenced_file()`：上一份/上个/刚才那个/最新的文件 → 本会话最近产物，否则工作文件夹最新文件；翻译、总结、转换共用。
3. **"我是说上一份文件的内容，" → "什么时候提醒你？"**——这句没有决定性句式，交给了分类模型，8B 判成 reminder，程序又信了它。两层修：分类结果里 reminder/memory 必须有用户自己的提醒/记忆词才作数；以"我是说/我的意思是/不对/不是"开头的话视为对上一个任务的更正，沿用上一个技能、把两句合并（事件 `follow_up`），不再重新分类。

回归 131 项通过；四个失败场景各有一条测试钉住。Ollama 调用现在记录 `load_seconds / total_seconds / thinking_chars`，用来查"深度思考 46 秒里只算了 4 秒"的去向。

## 0.7 标签行（2026-09-08，按"选项卡加闲聊"的方案）

**思路**：所有"傻"都出在程序猜用户想干什么；让用户点一下标签再打字，猜测这一层整个没了，7B 只做变形。输入框上方一排 11 个标签（翻译 · 总结 · 改写润色 · 问文件 · 提取成表 · 按要点写 · 转格式 · 记一下 · 提醒 · 查一下 · 聊聊天），点了标签就锁定技能、跳过路由和分类模型；每个标签带自己的小选项（翻成 / 长度 / 怎么改 / 写什么 / 结果格式 / 去哪里找 / 记到 / 表格列名）；"选文件…"从工作文件夹多选，选中的文件直接进槽位，不再问"哪个文件"；不点标签直接打字的老路保留。

**后端**：`POST /api/chat` 多收 `skill / files / options`，写进任务的 `intent.forced`；`TaskRun.classify()` 看到 forced 就不调分类模型；`/api/state` 多给 `skills` 与 `skill_options`；`/api/desktop` 多给 `last_artifact`（"刚生成的文件"）。新增三条技能：

| 技能 | 程序保证 |
|---|---|
| `rewrite` 改写润色 | 九种"怎么改"各有明确指令；数字必须原样出现；"只改错别字和标点"时长度只允许 ±20%；不过重做一次再标注 |
| `ask_file` 问文件 | 答案必须附一段一字不差的原文；引用核对不过或数字无来源就标"请对照原文确认"；文件里没有的答"没有提到" |
| `extract` 提取成表 | 列名来自选项或句子；每个格子的值必须在原文里出现，否则留空并计数；结果直接显示或写 Excel/CSV |

规则路由也认这三类句式（改写/润色/正式一点…；xx 里的 A、B 提取成表；文件里…是多少）。

**文案**：全部按润色稿替换（标签、提示语、程序问句、完成回复、状态栏、各页说明、模型页、桌宠气泡、执行记录步骤名）。Python 侧改了 `skills.py / loop.py`，Godot 侧改了 `pet.gd`。

**验证**：回归 138 项通过（新增标签强制、`/api/chat` 带 skill 的接口用例、三条新技能各一组）；`pet.gd` 过 Godot 4.4.1 语义检查。真实模型用例 28–32（标签锁定、改写、问文件×2、提取成表）：

```
.venv/bin/python tests/live_acceptance.py --tier 8b --only 12,14,21,24,25,28,29,30,31,32
```

**顺带修的**：启动器的健康探测改为看后端每秒写的心跳文件（`data/heartbeat`），HTTP 探测只作兜底且不走系统代理——之前开着 VPN 时探本机会被代理吞掉，启动器误判后端死了而反复重启，这就是"连接中断，正在重连"的真正来源；后端加了事件循环看门狗，卡超过 3 秒会把卡住的代码行写进 `desktop.log`。

### 0.7.1 界面整体收拾（同日）

- 标签行首版有布局错误（标签被拉成竖条、说明文字一字一行）：流式容器里的元素改为居中不拉伸，行内说明改为不换行的标签。
- 删掉用不上的：浏览器面板 `static/`（及 `/` 和 `/static/` 路由、`run.py` 自动开浏览器、两个只起后端的旧启动脚本）；提醒页的"内容 + N 分钟后"手工行（一句话设提醒已覆盖）；文件页的"总结 / 转成 PDF"按钮（标签行已覆盖），改为"用它来对话"（把选中的文件带进标签行，可多选）；输入框下的"添加文件…"（"选文件…"里有）；"动作"按钮和陪伴页的"动画控制面板"只在 `--animation-test` 启动参数下出现。
- 旧文件在 Mac 上移到 `05_临时测试归档/2026-09-08_0.7删除的旧文件/`。

## 0.8 界面重做 + 自测台（2026-09-08）

**我这边能看见界面了。** 云端容器装了虚拟显示器（Xvfb + Mesa 软件渲染），拉来了桌宠的全部动画资源，后端加了一个"演示模型"（`LULU_FAKE_MODEL=1`，`lulu/fakemodel.py`，对每种 schema 给出像样的假答复，界面上标"演示模型"），`desktop/tests/screenshot_tour.gd` 把工作窗口每一页、每个状态截图；`tests/ui_tour.sh` 一条命令跑完。以后前端改动先在这里看图，再给人看。

**新布局。** 左侧一条窄栏：lulu、五个页面（对话 · 文件 · 记忆 · 提醒 · 设置）、底部一个模型小标签（● 本地模型 / qwen3:8b / 深度思考 开）。主区只放当前页。对话页：会话下拉 + 新对话 + 执行记录 + "没做完的（n）"（只在有等你补充/未完成任务时出现，点开一个小窗继续或补充信息——原"任务"页取消）；消息按气泡显示（你在右、Lulu 在左，可选中复制）；底部输入区带标签行、每标签的小选项、选文件。字号：正文 16、标签 15、页标题 22、栏目 17。设置页 = 模型 + 联网搜索 + 陪伴（天气）。所有 12 号小字提到 14。

**天气自动定位。** 没选城市时按 IP 定位（ipapi.co，失败退 ip-api.com），拿到城市就查 Open-Meteo；第一次拿到晴/雨/阴就直接播放对应开场（`animation_state.play_weather_opening()`），之后照旧作为休息场景。手动选城市永远优先。系统本身没有可用的"系统天气"接口（Windows 小组件的天气不开放，macOS WeatherKit 要苹果签名），所以走这条路。

**顺带。** 标签按下去的字色之前几乎看不见（主题里 pressed 态用了浅色），已改深；启动器与看门狗见 0.7。

回归 138 项通过；`pet.gd` / `animation_state.gd` 过 Godot 语义检查；截图见 `03_交接与需求/界面截图-0.8/`。真机仍要看一次：桌宠开场动画、原生文件选择框、系统代理下的连接。

### 0.8.1 配色与前端走查（同日）

- 配色改为低饱和的暖白/沙色：背景 #faf7f1、左栏 #f1ebe0、按钮 #efe4cf、按下 #d9b978、正文 #3d342a、次要文字 #7d6e5d；字号正文 15–16、控件 14、页标题 20。
- `desktop/tests/tag_tour.gd`：像人一样在工作窗口里点标签、选选项、选文件、打字、发送，共 18 个用例（11 个标签 + 空要点、缺文件、本地/联网查询、不点标签的自由输入），每个用例读回后端任务核对技能、文件、选项是否送到、回复是否出现在对话里。`TOUR=tag_tour tests/ui_tour.sh` 一条命令跑。演示模型下 18/18 通过。
- 走查时顺手修的：选了文件不打字时用户气泡显示"处理已选文件"→ 改成"提取成表：名单.md"这样的一句；按要点写在有"写什么"选项时文件名用文体（周报.docx），不再从正文截一句当文件名；任务完成后顶部状态行不再重复显示回复。
- 演示模型只能验证前端到后端的管子；技能本身的质量仍要在 Mac 上用真实模型跑 `live_acceptance.py --tier 8b --only 12,14,21,24,25,28,29,30,31,32`。

## 0.9 打包收口（2026-09-08）

**先把仓库建了、CI 跑绿，再动打包。** `github.com/aceaaa777/lulu-agent`（私有）。`ci.yml`：ubuntu / windows / macos 三系统回归 + 演示模型冒烟 + Godot 4.4 脚本检查。第一次 Windows 就抓出三个只在 Windows 上会炸的真问题：命令行后端用 Unix 规则拆命令（`C:\...` 的反斜杠被吃掉，`CommandProvider.split()` 改为按平台）；子进程管道默认用系统代码页，中文一出就崩（强制 `PYTHONIOENCODING=utf-8` / `PYTHONUTF8=1`）；文件相对路径带反斜杠（`files.py` 统一 `as_posix()`）。现在 139+3 项三系统全绿。

**桌宠不再带 Godot 编辑器，改为导出版。** `desktop/export_presets.cfg` 四个预设：Linux / Windows Desktop / macOS 三个主程序（不含动画帧，70–95MB）+ `frames`（只含 `assets/optimized`，导成 `frames.pck`，404MB）。`packaging/export_pet.py` 一条命令 headless 导出，导出模板缺就自动下载（1.2GB，一次）。`pet.gd` 启动时 `load_frame_pack()`：开发树里直接有帧就不管；否则按 `LULU_FRAMES` 环境变量、可执行文件旁、上一级、`Lulu.app/Contents/Resources` 找 `frames.pck` 并 `load_resource_pack`；找不到不刷屏（`FrameStore.disabled`），桌宠状态栏提示"缺少动画资源包"，工作窗口照常能用。macOS 导出需要 `import_etc2_astc=true`（project.godot 已加，帧都是无损模式，不影响导入）。

**启动器认导出版。** `desktop_launcher.py`：`package_root()` 从可执行文件往上找到含 `pet/` 或 `desktop/` 的目录；`find_pet()` 先找 `pet/Lulu.exe` / `pet/Lulu.app` / `pet/Lulu.x86_64`，没有才退回 Godot 编辑器 + 源码树（开发用）；`pet/frames.pck` 存在就通过 `LULU_FRAMES` 传给桌宠；Windows 上子进程加 `CREATE_NO_WINDOW`（冻结成无控制台程序后 ollama/后端不再弹黑窗）。

**运行环境冻结。** Python 用 PyInstaller 冻成 `runtime/LuluRuntime/`（`--onedir`，Windows 加 `--noconsole`），用户不用装 Python。冻结只能在目标系统上做 → `release.yml`：`pet`（ubuntu 导出三平台）→ `runtime`（windows-latest / macos-latest 各冻一份并冒烟启动后端）→ `package`（组装 轻量版/完整版，推 `v*` 标签时自动挂成草稿 Release）。

**包的形状。** `packaging/build_release.py --platform windows|macos|linux [--runtime …]` → `dist/Lulu-<版本>-<平台>/`：`安装并启动 Lulu.command|.cmd`（+ `tools/安装并启动.ps1`）、`先看这里.md`、`release.json`、`pet/`（主程序 + `frames.pck` 动画包 + `frames.sha256`）、`runtime/`（CI 产物；没有就是源码版，需 Python 3.11+）、`agent/`（lulu 源码 + 脚本，几 MB）。zip 保留 Unix 权限与符号链接，附 `.sha256`。**只有一种包，动画包永远在里面**（用户提出：动画演示层是核心，不能靠安装时下载）；`--lite` 只是实验开关，安装脚本里的『缺 frames.pck 就从 Release `assets-v1` 补下载并校验』留作修复手段。

**一键安装脚本做的事（幂等，每次双击都行）：** 动画包缺就下载校验 → macOS 去隔离标记 + 本机 ad-hoc 签名 / Windows `Unblock-File` → 有 runtime 用 runtime，没有就找 Python 3.11+ 建 `agent/.venv`（Windows 没有就提示 winget 装）→ 没 Ollama 只提示不阻塞；有 Ollama 没模型就问一句再拉（复用 `安装本地模型.*`）→ 后台启动。模型永远不进包（5GB）。

**验证。** 云端 Xvfb 下：导出的 Linux 桌宠 + `frames.pck` 连上演示模型后端，截图里桌宠正常（`frames_loaded` 462）；去掉 `frames.pck` 时安静降级；整包（源码版和冻结版各一次）用 `安装并启动 Lulu.command` 从零跑通到桌宠出现。`tests/test_packaging.py` 锁住包结构和启动器的查找逻辑。macOS 的签名/隔离流程、Windows 的 SmartScreen 与 `.cmd`→`.ps1` 链只能真机看：Mac 先跑 `python packaging/build_release.py --platform macos --full` 再双击；Windows 等 `release.yml` 产出的包在 TUF 上装。

**还没做 / 要你定：** `LICENSE`（MIT 或 Apache-2.0）；公开前把 `交接/` 里的桌面路径清掉；程序图标（Windows 导出 icon 需要 rcedit，先用默认）。

### 0.9.1 按 GPT 真机测试报告修（2026-09-08 晚，报告：`~/Documents/ChatGPT/开源/Lulu测试_2026-09-08/给Claude_Lulu0.9真机测试报告.md`）

报告质量很高：43 条任务、每条有截图和数据库事件、真/演示模型对照，6 个程序问题 + 1 个模型问题，11 标签各 8 过 3 不过。逐条：

- **P01 文件列表点一下全白** — 程序。Godot 的 `ItemList` 把 `focus` 和 `cursor` 两个样式盒画在行文字**上面**，我给它们填了不透明底色，于是一获得焦点整张列表被盖住。改成只画边框（`draw_center=false`）。顺手修了一个报告没点名但肯定在作怪的：每 12 秒的刷新会 `clear()` 重建文件/记忆列表，选中状态随之丢失；现在行内容没变就不重建，变了也按文字把选中放回去（`fill_list`）。
- **P02 "直接显示"仍生成 Word** — 程序。标签行把"直接显示"当默认值不发送，后端拿不到 `out_format` 就走默认 `docx`。现在标签驱动的按要点写：没指定格式、句子里也没要文件 → 正文直接进对话，不落盘（`generate_inline` 事件）；选了 Word/PDF/Markdown 才写文件。
- **P03 Excel 问答 34 件/314 元** — 程序（报告判断正确）。表格证据只给模型列统计，而统计把"合计"行又加了一遍，交付状态这种文字列根本没进 prompt。`files.analyze()` 现在：首格是 合计/总计/小计/Total 的行不进统计（记在 `excluded_rows`），并把整张表的单元格文本一起带上（`text`，8000 字内）；问文件把行文本 + "程序按 N 行数据统计，不含合计行：…"一起给模型，模型不再自己加数。
- **P04 选了 Excel 提取成表却说没文件** — 程序。提取只认 `file` 类证据，表格是 `table` 类，于是当成没材料 0.01 秒就反问。现在两类都算，行文本作为原文。
- **P05 设置页被轮询打回** — 程序。轮询在设置页打开时每次重新 `load_backend()`，把下拉框拨回已保存值。改为：进入设置页时加载一次、点"应用"后加载，轮询不再碰它。
- **P06 删记忆的确认文案** — 这是设计而不是 bug：精准删除已经只删用到过该记忆的那几条对话和任务上下文（`store.forget` 按 `memory_refs`），但用户自己说出那条信息的原话没法按引用找到，所以还会把此前对话移出模型的参考范围（`epoch`），对话记录本身不删、界面上还在。原文案"清空对话和任务上下文"说重了，改成如实描述。
- **M01 邮件擅自加了个错日期** — 报告归为模型问题，但程序侧可以堵：撰写器 prompt 现在带 `今天：2026-09-08 周二`，并明确"用户写的时间词原样保留，不要换算成具体日期；确需换算以今天为准并保证星期对得上"。8B 没有日历，之前是纯猜。

新增回归：`test_generate_direct_display_writes_no_file`、`test_table_analysis_skips_total_rows_and_keeps_the_cells`、`test_ask_file_on_excel_reads_the_rows_not_only_the_sums`、`test_extract_from_selected_excel_does_not_ask_for_a_file`（用报告里的采购记录数据）；截图巡视加了"文件页选中一行"和"设置页改成 API → 轮询两次仍保持"两个镜头。147 项通过，标签巡视 18/18。

报告里"没测到的"清单（新下载态的 Gatekeeper、三种天气开场、多显示器/休眠、4B 档、真实 API 钥匙）仍然没测；其中 Gatekeeper 那条要等 CI 产出的包从网上下下来再看。
